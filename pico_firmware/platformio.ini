; PlatformIO configuration file
; Specifies board type (Raspberry Pi Pico)
; Lists libraries/dependencies
    ; will automatically generated in the library folder 
; Compiler settings and upload configuration

; can find a template for this 
[env:pico_w]
platform = raspberrypi
board = pico_w
framework = arduino
monitor_speed = 115200
