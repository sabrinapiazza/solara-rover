{
  "name": "Agricultural Rover - ROS2 Jazzy",
  "image": "osrf/ros:jazzy-desktop-full",
  
  "features": {
    "ghcr.io/devcontainers/features/github-cli:1": {}
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-iot.vscode-ros",
        "ms-vscode.cpptools",
        "twxs.cmake",
        "redhat.vscode-yaml"
      ]
    }
  },

  "postCreateCommand": "apt-get update && apt-get install -y python3-pip git bluez python3-dbus libdbus-1-dev && pip3 install bless && cd ros2_ws && source /opt/ros/jazzy/setup.bash && colcon build --symlink-install",

  "remoteUser": "root",
  
  "runArgs": [
    "--net=host",
    "--cap-add=NET_ADMIN",
    "--cap-add=NET_RAW",
    "--volume=/var/run/dbus/system_bus_socket:/var/run/dbus/system_bus_socket"
  ]
}
